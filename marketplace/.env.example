# Supabase — project: adamhall-marketplace (ap-southeast-2)
NEXT_PUBLIC_SUPABASE_URL=https://ocyxhfyphqyirjbyvhnw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never expose to the browser. Used for tokenised status reads,
# submission photo signed URLs, and admin server actions.
SUPABASE_SERVICE_ROLE_KEY=

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Email (Resend). Leave empty in dev — emails log to console instead.
RESEND_API_KEY=
EMAIL_FROM="Adam Hall <adam@example.com.au>"

# Admin notification inbox
ADMIN_NOTIFY_EMAIL=
